<nav class="navbar">
  <div class="container">
    <a href="/" class="logo">{{ section.settings.logo }}</a>
    <span class="hamburger">
      <i class='bx bx-menu'></i>
    </span>
    <ul class="nav_menu">
      {%- for link in linklists.main-menu.links -%}
      <li><a href="{{ link.url }}">{{ link.title }}</a></li>
      {%- endfor -%}
    </ul>
  </div>
</nav>